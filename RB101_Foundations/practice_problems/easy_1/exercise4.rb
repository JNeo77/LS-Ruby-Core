numbers = [1, 2, 3, 4, 5]

numbers.delete_at(1)
# Removes the item at index '1', in this case the number '2'
numbers.delete(1)
# Removes the item that matches the argument, in this case the number '1'